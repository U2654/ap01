#ifndef geometry_h
#define geometry_h

/* returns area of circle for argument radius. */
float area_of_circle(float radius);

/* returns circumference of circle for argument radius. */
float circumference_of_circle(float radius);

#endif